/*
import 'package:flutter/material.dart';

class CreateFood extends StatelessWidget {
  const CreateFood({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

In case we create a food, we must specify each of its `condiments` and the quantity involved
For drink no need to.
Also when buying (when an order is placed) deducted for each food the quantity of portions of each of its condiments. Thus food does not have a stock and drink does not have portions.
*/
