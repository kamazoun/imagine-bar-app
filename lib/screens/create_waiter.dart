import 'package:flutter/material.dart';

class CreateWaiter extends StatelessWidget {
  const CreateWaiter({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
